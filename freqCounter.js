function same(arr1, arr2) {
  // is every value in the second array the square of the values in the first array?
  // Order can be mixed up
  // the frequency of vallues of both arrays must be the same.

  // create two objects that contain the frequency of each value for each array.
  // if 1,2,2,3,4 is in array1, then the object1 would = {1:1, 2:2, 3:1, 4:1}
  // do this using Non-Nested loops.
  // then run another non-nested loop checking
  // if the keys from the array2 are the squares of the keys from array1
  // then run another if statement within the same loop
  // checking if the frequencies are correct.
  return true;
}
